# Module 22: Platform Admin Specs

## Authorization
- Restricted to users where `user_profiles.role = 'admin'`.

## Views
- Platform stats: total users, active applications, university count, system activity logs.