# specs/

These files are **implementation notes** from an earlier prototype. They are not the product specification.

The single specification is `Global-Student-Cube-Complete-Development-Specification.md` at the repository root. Owner decisions in `SCOPE-ALIGNMENT.md` and `AGENTS.md` win when they disagree with that document.

Do not add screens, tables, or rules from this folder unless they already exist in the specification.
